---
# This is a sample template for {{ environment }} environment
AWSTemplateFormatVersion: "2010-09-09"
Resources:
  Basic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: "{{ topic_name }}"